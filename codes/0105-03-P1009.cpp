#include <iostream>

using namespace std;

int main() {

    /* 已注释的代码，m < 20 可行，但高精度要有高精度的运算。
    int m = 0;
    cin >> m;

    long long fact = 1;
    long long sum = 0;

    for(int i = 1; i <= m; i++){
        fact *= i;
        sum += fact;
    }

    cout << sum;
    */

    int m = 0;
    cin >> m;

    int fact[90] = {0};
    fact[0] = 1;
    int sum[90] = {0};

    for(int i = 1; i <= m; i++){
        
    }

    return 0;
}